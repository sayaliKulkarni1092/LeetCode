class Solution {
    public boolean isPalindrome(int x) {
        if (x < 0) {
            return false;
        }
        StringBuilder sb = new StringBuilder(String.valueOf(x));
        sb.reverse();

        if(x == Long.parseLong(sb.toString())) {
            return true;
        }
        return false;
        
    }
}
